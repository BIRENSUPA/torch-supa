# 安全策略

## 支持的版本

我们支持以下列出的最新维护版本。除非维护者明确声明，否则不会对旧版本进行回溯补丁。

| 版本 | 支持 |
| --- | --- |
| 2.12.0 | √ |
| 2.11.0 | √ |
| 2.10.0 | √ |
| 2.9.0 | √ |
| 2.8.0 | √ |
| 2.6.0 | √ |
| < 2.6.0 | × |

## 漏洞报告

**安全漏洞可以通过公开问题、公开拉取请求、公开讨论或以下列出的私有报告渠道进行报告。**

请使用最符合问题敏感程度的报告渠道：

- **公开问题、公开拉取请求或公开讨论**：适用于非敏感的安全报告、安全加固建议、文档问题或不涉及可利用漏洞细节的问题。

- **GitHub 私有漏洞报告**：`[插入您的 Gitee /security/advisories URL]`

- **私有邮箱**：[opensource@birentech.com](mailto:opensource@birentech.com)（如果无法使用私有漏洞报告）

报告时，请尽可能包含以下信息：

- 漏洞及其潜在影响的清晰描述

- 重现步骤，最好提供最小重现步骤

- 受影响的版本，例如 PyTorch 版本、torch-supa 版本、提交 ID 和 LKG 版本

- 环境详情，例如操作系统、Python 版本、驱动程序/运行时版本和硬件型号

- 该问题在默认配置下是否可利用

- 建议的缓解措施或补丁（如有）

请避免在报告中包含敏感凭据、生产机密、客户私有数据或无关的个人信息。

## 关于 PyTorch 模型的安全说明

本项目依赖于 PyTorch。请注意，**PyTorch 模型是程序**，运行不受信任的模型等同于运行不受信任的代码。如果您使用本项目加载或推理模型，请仅使用来自受信任来源的模型文件（例如，`.pth` 或 `.pt`）。从不受信任的来源加载模型可能会导致任意代码执行，这是 PyTorch 及其衍生项目的普遍风险。

## 预期流程

收到漏洞报告后，我们将遵循以下流程：

1. **确认**：我们将在 5 个工作日内尽力确认收到报告。

2. **初步评估**：我们将确认该报告是否在处理范围内，并可能要求您提供更多信息。

3. **评估**：我们将评估漏洞的严重性、受影响的版本、可利用性以及可能的缓解措施。

4. **修复**：如果问题得到确认，我们将着手制定修复或缓解方案。

5. **协调披露**：在发布公告之前，我们将与报告者协调确定合适的披露时间表。

> **法律声明**：以上时间表和流程仅为预期目标。项目维护者不对响应、修复或披露时间表做出任何合同或法律保证。本政策中的任何内容均不构成对项目或其贡献者的任何可强制执行的义务。

## 范围

**本项目涵盖的漏洞**包括以下仓库组件中的安全问题：

- `torch_supa/`

- `_inductor/`

- `backends/`

- `cmake/`

- `contrib/`

- `csrc/`

- `distributed/`

- `multiprocessing/`

- `profiler/`

- `supa/`

- `testing/`

- `utils/`

- `docs/`

本项目涵盖的问题示例包括：

- 项目维护的本地代码中的内存损坏或不安全的内存访问

- 本项目引入的权限边界违规

- 在受支持的配置下，不安全的输入处理可能导致代码执行

- 项目提供的构建、运行时或打包逻辑中存在安全敏感的错误配置

**本项目不涵盖的问题**包括：

- 我们无法直接修复的第三方依赖项中的漏洞。请将这些问题报告给上游。如果在项目层面存在可行的缓解措施，我们仍可在此处跟踪问题。

- PyTorch 本身的漏洞，应直接报告给 PyTorch 社区。

- 加载不受信任或恶意构造的模型文件导致的安全问题。请参阅[PyTorch 模型安全说明](#security-note-on-pytorch-models)。

- 需要物理访问开发人员机器或已入侵的本地环境的问题。

- 针对此软件默认部署的理论攻击，但不存在实际的利用路径。

- 自托管环境中的配置错误或漏洞，偏离了已记录的安全默认设置。

- 针对项目维护者或用户的社会工程或网络钓鱼攻击。

- 仅基于资源消耗过大而没有实际安全影响的拒绝服务报告。

## 负责任的研究指南

在测试或报告安全问题时，请：

- 仅在您有权使用的系统和环境中进行测试。

- 避免侵犯隐私、数据损毁、服务中断和生产服务性能下降。

- 除证明问题所需外，不得泄露其他数据。